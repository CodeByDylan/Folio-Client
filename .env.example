# Base URL of the Folio API. Server-only; never reaches the browser bundle.
FOLIO_API_URL=https://api.dylandebeer.nl

# Branding shown in the shell. Public: inlined into the client bundle at build time.
VITE_APP_NAME=Folio
VITE_APP_LOGO=/images/logo.svg
